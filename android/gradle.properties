org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# ── Build speed optimizations ──────────────────────────────────────────────────
# Daemon drží JVM v paměti — eliminuje ~5–10s při každém spuštění
org.gradle.daemon=true
# Paralelní build modulů (každý plugin = vlastní modul)
org.gradle.parallel=true
# Mezipaměť výsledků — nezměněné moduly se nekompilují znovu
org.gradle.caching=true
# Konfiguruje pouze potřebné subprojekty
org.gradle.configureondemand=true
